You should make it so the function callers to add numbers or special characters or dashes is pushed into an array, then a randomizer randomly picks how often to call that function.  Right now each of the functions are being called in order.